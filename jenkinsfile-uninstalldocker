pipeline {
  agent any

  environment {
    ANSIBLE_HOST = "20.198.179.127"
    ANSIBLE_USER = "boho"
    REPO_URL     = "git@github.com:kitsanaphon1/install-docker-deploy.git"
    WORKDIR      = "/home/boho/projects/install-docker-deploy"  // ✅ เปลี่ยน path ตรงนี้
  }

  stages {

    stage('📂 Copy VM IP from Terraform Job') {
      steps {
        copyArtifacts(
          projectName: 'terraform',
          filter: 'vm_ip.txt',
          selector: [$class: 'StatusBuildSelector']
        )
      }
    }

    stage('📤 Send vm_ip.txt to Ansible VM') {
      steps {
        sshagent(credentials: ['ssh-ansible-agent']) {
          sh '''
            scp -o StrictHostKeyChecking=no vm_ip.txt ${ANSIBLE_USER}@${ANSIBLE_HOST}:~/vm_ip.txt
          '''
        }
      }
    }

    stage('📥 Clone fresh repo on Ansible VM') {
      steps {
        sshagent(credentials: ['ssh-ansible-agent']) {
          sh '''
            ssh -o StrictHostKeyChecking=no -A ${ANSIBLE_USER}@${ANSIBLE_HOST} '
              echo "🧹 ลบ repo เก่าถ้ามี..."
              rm -rf ${WORKDIR}

              echo "📥 Clone ใหม่จาก GitHub..."
              git clone ${REPO_URL} ${WORKDIR}
            '
          '''
        }
      }
    }

    stage('🧹 Run Uninstall Playbook via Ansible') {
      steps {
        sshagent(credentials: ['ssh-ansible-agent']) {
          sh '''
            ssh -o StrictHostKeyChecking=no -A ${ANSIBLE_USER}@${ANSIBLE_HOST} '
              export ANSIBLE_HOST_KEY_CHECKING=False
              source ~/ansible-env/bin/activate
              cd ${WORKDIR}/playbooks
              ansible-playbook uninstall-docker.yaml -i "$(cat ~/vm_ip.txt)," -e ansible_user=azureuser
            '
          '''
        }
      }
    }

    stage('🧽 Cleanup repo after execution') {
      steps {
        sshagent(credentials: ['ssh-ansible-agent']) {
          sh '''
            ssh -o StrictHostKeyChecking=no ${ANSIBLE_USER}@${ANSIBLE_HOST} '
              echo "🧽 ลบโฟลเดอร์หลังใช้งาน..."
              rm -rf ${WORKDIR}
            '
          '''
        }
      }
    }

  }
}
